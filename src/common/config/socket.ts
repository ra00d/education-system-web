// import { isMobile } from "@/lib/utils";
import { io as socketIO } from "socket.io-client";
// const ws = "http://127.0.0.1:3000"; //isMobile() ? "http://192.168.0.235:3000" : "http://127.0.0.1:3000";

// export const socket = socketIO(ws, { secure: true });
